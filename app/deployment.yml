# deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: will-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: will
  temhjk4twlheuirth123nqw:
    metadata:
      labels:
        app: will
    spec:
      containers:
      - name: will
        image: wil42/playground:v2
        ports:
        - containerPort: 8888
