apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  rules:
  - host: 127.0.0.1
    http:
      paths:
      - path: "/app"
        pathType: Prefix
        backend:
          service:
            name: will-service
            port:
              number: 8888

